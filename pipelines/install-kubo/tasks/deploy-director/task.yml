---

platform: linux

image_resource:
  type: docker-image
  source: {repository: czero/cflinuxfs2}

params:
  GOVC_INSECURE:
  GOVC_URL:
  GOVC_USERNAME:
  GOVC_PASSWORD:
  GOVC_DATACENTER:
  GOVC_DATASTORE:
  GOVC_NETWORK:
  GOVC_RESOURCE_POOL:
  GOVC_HOST:

inputs:
  - name: homelab
  - name: kubo-deployment
  - name: kubo-environment

outputs:
  - name: kubo-environment

run:
  path: homelab/pipelines/install-kubo/tasks/deploy-director/task.sh
