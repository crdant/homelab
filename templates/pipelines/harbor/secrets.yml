credentials:
- name: ${pipeline_secret_root}/admin_password
  type: password
  value: ${admin_password}
- name: ${pipeline_secret_root}/server_certificate
  type: certificate
  value:
    certificate: |
      ${certificate}
    private_key: |
      ${private_key}
