credentials:
# credhub keys
- name: ${pipeline_secret_root}/credhub_encryption_key_1
  type: password
  value: ${credhub_encryption_key_1}
- name: ${pipeline_secret_root}/credhub_encryption_key_3
  type: password
  value: ${credhub_encryption_key_2}
- name: ${pipeline_secret_root}/credhub_encryption_key_3
  type: password
  value: ${credhub_encryption_key_3}
- name: ${pipeline_secret_root}/server_certificate
  type: certificate
  value:
    certificate: |
      ${certificate}
    private_key: |
      ${private_key}
