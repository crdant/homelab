credentials:
- name: ${pcf_team_secret_root}/pivnet_token
  type: password
  value: ${pivnet_token}
- name: ${pcf_team_secret_root}/github_token
  type: password
  value: ${github_token}
- name: ${pcf_team_secret_root}/opsman_admin_username
  type: value
  value: ${opsman_admin_username}
- name: ${pcf_team_secret_root}/opsman_admin_password
  type: password
  value: ${opsman_admin_password}
