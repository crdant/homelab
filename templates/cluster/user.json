{
  "kind": "tm:auth:user:userstate",
  "name": "${username}",
  "fullPath": "${username}",
  "description": "Pivotal Cloud Foundry API account",
  "password": "${password}",
  "partitionAccess": [
    {
      "name": "${partition}",
      "role": "operator"
    },
    {
      "name": "${apps_partition}",
      "role": "operator"
    }
  ]
}
