{
  "kind": "tm:auth:user:userstate",
  "name": "${username}",
  "fullPath": "${username}",
  "description": "Pivotal Cloud Foundry API account",
  "password": "${password}",
  "partitionAccess": [
    {
      "name": "${partition}",
      "role": "operator",
      "nameReference": {
        "link": "https://localhost/mgmt/tm/auth/partition/${partition}?ver=13.1.1"
      }
    }
  ]
}
